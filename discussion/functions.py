def mult(x,y):  # defining my function as "mult", takes in two variables (x, y)
    # mult is going to return the product of x and y 
    return (x * y)  

print(mult(50, 392)) # will print in terminal when I run this script

def mult(x, y):
    # can have an intermediate variable to store the value of (x, y)
    prod = x * y
    return(prod) # instead of returning (x * y), just going to return product
print(mult(1, 2))

